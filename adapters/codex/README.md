# Codex Adapter

Codex-specific runtime and installation assets live here.

The included `sol-luna-xi` package is intentionally vendor-specific because it pins OpenAI model IDs, reasoning levels, Codex custom-agent definitions, sandbox modes, and Codex installation paths.

Portable skills should remain under the repository-level `skills/` directory.
